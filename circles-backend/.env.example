DATABASE_URL=postgresql://user:password@localhost:5432/circles
GEMINI_API_KEY=your-gemini-api-key
FIREBASE_PROJECT_ID=your-firebase-project-id
SECRET_KEY=your-secret-key

# Comma-separated list of origins allowed to call the API from a browser.
# Origins must include the scheme (https://). Leave unset to use the defaults
# in app/core/config.py (localhost:3000 plus the known Vercel domains).
ALLOWED_ORIGINS=http://localhost:3000,https://circles-9ez5.vercel.app
